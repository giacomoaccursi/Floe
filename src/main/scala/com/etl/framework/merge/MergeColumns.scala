package com.etl.framework.merge

/**
 * Column names used internally by merge operations
 */
private[merge] object MergeColumns {
  val ROW_NUM = "_row_num"
  val COMPARE_KEY = "_compare_key"
  val COUNT = "_count"
}
