paths:
  outputPath: "example/output"
  rejectedPath: "example/output/rejected"
  metadataPath: "example/output/metadata"

processing:
  batchIdFormat: "yyyyMMdd_HHmmss"
  failOnValidationError: false
  maxRejectionRate: 0.15

performance:
  parallelFlows: true
  parallelNodes: true
